{
  "name": "@pagespatial/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Control-plane API: job ledger, dispatch, reconciler. Not the parsing library.",
  "engines": {
    "node": ">=25"
  },
  "scripts": {
    "migrate": "node src/migrate.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "modal": "0.9.0",
    "pg": "8.16.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.6"
  }
}
